extends layout.pug
append head
  link(rel="stylesheet" href="/stylesheets/delete-comment.css")
block content
  p=comment.content
  div
    p Proceed with deleting this story?
  div
    form(action=`/posts/${comment.id}/delete` method='post')
      input(type='hidden' name='_csrf' value=csrfToken)
      button(class='btn-delete' type='submit') Delete Story
      a(href=`/posts/${comment.id}` class="btn-cancel") Cancel
